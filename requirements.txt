# Workshop-pinned Python environment.
# Verified against task2_reference_solver.py on macOS / Python 3.12.
# Re-pin if you re-verify the reference solver on a new combination.
#
# Install with:
#   uv pip install -r requirements.txt
#
# Or in one go with a fresh venv:
#   uv venv ~/.venv --python 3.12
#   source ~/.venv/bin/activate
#   uv pip install -r requirements.txt

numpy==2.2.6
scipy==1.17.1
pandas==3.0.1
matplotlib==3.10.9
scikit-learn==1.8.0
scikit-fem==12.0.1
